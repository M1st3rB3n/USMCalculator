{% extends 'base.html.twig' %}

{% block title %}Importer la base de données{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-warning text-dark">
                    <h4 class="mb-0"><i class="bi bi-upload"></i> Importer une base de données</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-danger">
                        <i class="bi bi-exclamation-triangle"></i> <strong>Attention !</strong> L'importation d'un fichier écrasera la base de données actuelle. Cette action est irréversible (une sauvegarde automatique sera toutefois créée dans <code>var/data/data.db.bak</code>).
                    </div>

                    <form action="{{ path('app_database_import') }}" method="post" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="database_file" class="form-label">Sélectionner le fichier de base de données (.db)</label>
                            <input class="form-control" type="file" id="database_file" name="database_file" accept=".db" required>
                        </div>
                        <div class="d-flex justify-content-between">
                            <a href="{{ path('app_home') }}" class="btn btn-secondary">Annuler</a>
                            <button type="submit" class="btn btn-warning" onclick="return confirm('Êtes-vous sûr de vouloir remplacer la base de données actuelle ?')">Importer la base</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
