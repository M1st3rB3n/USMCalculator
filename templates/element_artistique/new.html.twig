{% extends 'base.html.twig' %}

{% block title %}Ajouter un élément artistique{% endblock %}

{% block body %}
<div class="container">

    <div class="card shadow">
        <div class="card-header bg-success text-white">
            <h1 class="h3 mb-0">Ajouter un élément artistique</h1>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
                {{ form_row(form.nom) }}
                <div class="row">
                    <div class="col-md-6">{{ form_row(form.score) }}</div>
                    <div class="col-md-6">{{ form_row(form.QoE) }}</div>
                </div>
                <div class="d-flex justify-content-between mt-3">
                    <a href="{{ path('app_element_artistique_index') }}" class="btn btn-secondary">Annuler</a>
                    <button type="submit" class="btn btn-success">Enregistrer</button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
