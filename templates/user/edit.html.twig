{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card shadow">
        <div class="card-header bg-warning text-dark">
            <h1 class="h3 mb-0">Modifier l'utilisateur : {{ user.email }}</h1>
        </div>
        <div class="card-body">
            <p class="text-muted mb-4 small">Laissez le mot de passe vide si vous ne souhaitez pas le modifier.</p>
            {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
</div>
{% endblock %}
